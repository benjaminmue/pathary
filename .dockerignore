# Git
.git/
.gitignore
.gitattributes

# Environment and secrets
.env
.env.*
!.env.example

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Development and testing
tests/
settings/phpcs.xml
settings/phpstan.neon
settings/psalm.xml
settings/phpunit.xml
phpunit.xml
.phpunit.result.cache
coverage/

# Documentation
*.md
!README.md
docs/

# Docker files (not needed in image)
Dockerfile
docker-compose*.yml
.dockerignore

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml

# Node (if any frontend build was done locally)
node_modules/

# Vendor (will be rebuilt in container)
vendor/

# Storage and cache (will be mounted as volumes)
storage/logs/*
storage/app/*
storage/profile-images/*
public/images/cached/*

# Keep directory structure with .gitignore files
!storage/.gitignore
!storage/app/.gitignore
!storage/app/public/.gitignore
!storage/logs/.gitignore

# Security audit report (development artifact)
security_audit_report.txt
