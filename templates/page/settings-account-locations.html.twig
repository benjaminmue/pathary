{% extends 'base.html.twig' %}

{% block title %}
    {{ t('settings_account.title_general') }}
{% endblock %}

{% block stylesheets %}
    <link href="{{ applicationUrl }}/css/settings.css" rel="stylesheet">
{% endblock %}

{% block scripts %}
    <script src="{{ applicationUrl }}/js/settings-account-location.js"></script>
{% endblock %}

{% block body %}
    <main role="main" class="container">
        {{ include('component/navbar.html.twig') }}

        <div style="text-align: center">
            {{ include('component/settings-nav.html.twig') }}

            <div style="padding-top: 1rem">
                <h5 class="text-center">{{ t('settings_account.locations') }}</h5>

                <div style="padding-bottom: .8rem">
                    <p>{{ t('settings_account.locations_desc') }}</p>

                    {% if locationsEnabled %}
                        <button type="button" class="btn btn-outline-danger" onclick="toggleLocationFeature()" id="toggleLocationsFeatureBtn">{{ t('settings_account.disable_locations') }}</button>
                    {% else %}
                        <button type="button" class="btn btn-primary" onclick="toggleLocationFeature()" id="toggleLocationsFeatureBtn">{{ t('settings_account.enable_locations') }}</button>
                    {% endif %}
                </div>

                <div id="locationAlerts"></div>

                <div class="table-responsive">
                    <table class="table table-hover" id="locationsTable">
                        <thead>
                        <tr>
                            <th scope="col">{{ t('settings_account.manage_locations') }}</th>
                        </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <div class="spinner-border d-none" role="status" id="locationsTableLoadingSpinner" style="margin-bottom: .5rem">
                    <span class="visually-hidden">{{ t('settings.loading') }}</span>
                </div>

                <button type="button" class="btn btn-primary" style="width: 100%" onclick="showCreateLocationModal()" id="createLocationBtn" {% if locationsEnabled == false %}disabled{% endif %}>{{ t('settings_account.add_location') }}</button>
                <br>
            </div>
        </div>

        {{ include('component/modal-location.html.twig') }}
    </main>
{% endblock %}
