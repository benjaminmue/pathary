# Pathary Scheduled Sync
# Runs daily at 2:00 AM to sync TMDB metadata and OMDb ratings
# Only syncs movies not updated in 7+ days (max 1000 per day)

# Run scheduled sync daily at 2:00 AM via wrapper script
0 2 * * * /bin/bash /app/build/scripts/run-scheduled-sync.sh
