# Pathary Scheduled Sync
# Runs daily at 2 AM to sync TMDB metadata and OMDb ratings
# Only syncs movies not updated in 7+ days (max 1000 per day)

# Run scheduled sync daily at 2:15 AM (temporarily for testing)
15 2 * * * www-data cd /app && /usr/local/bin/php bin/console.php sync:scheduled >> storage/logs/scheduled-sync.log 2>&1
